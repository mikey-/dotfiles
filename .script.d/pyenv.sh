#!/usr/bin/env bash

# initialise python env
# this allows pyenv to automatically change python versions
#if command -v pyenv 1>/dev/null 2>&1; then
#  eval "$(pyenv init -)";
#  eval "$(pyenv virtualenv-init -)";
#fi
